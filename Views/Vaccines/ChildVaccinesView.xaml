<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ACS_View.ViewModels"
             xmlns:model="clr-namespace:ACS_View.Domain.Entities"
             x:DataType="vm:VaccinesPageViewModel"
             x:Class="ACS_View.Views.Vaccines.ChildVaccinesView">

    <ContentView.Resources>
        <Style x:Key="title" TargetType="Label">
            <Setter Property="HorizontalOptions" Value="Center"/>
            <Setter Property="VerticalOptions" Value="Center"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="TextColor" Value="Black"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="LineBreakMode" Value="WordWrap"/>
        </Style>
    </ContentView.Resources>

    <VerticalStackLayout
        BindingContext="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Vaccines}"
        x:DataType="model:Vaccines">
<!-- Vacinas infantis -->
                <Grid
                    RowDefinitions="*,*,*,*,*,*,*,*,*,*,*,*,*,*"
                    IsVisible="{Binding IsChild}">

                    <!-- ao nascer -->
                    <Border
                        IsVisible="{Binding ShowRN}"
                        Grid.Row="0"
                        Margin="15,0,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*,*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="2"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SituacaoRN}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="Ao nascer"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="BCG"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="BCG_Infantil"/>
                                </Label.GestureRecognizers>
                                
                            </Label>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="Hepatite B"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="HB"/>
                                    
                                </Label.GestureRecognizers>
                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="Única"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Única"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 2 meses -->
                    <Border
                        IsVisible="{Binding Show2Meses}"
                        Grid.Row="1"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*,*,*,*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="4"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao2Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                Text="2 meses"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- PENTA -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Penta"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Penta1"/>
                                </Label.GestureRecognizers>
                                
                            </Label>

                            <!-- VIP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="VIP"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VIP1"/>
                                </Label.GestureRecognizers>

                            </Label>
                            
                            <!-- Pneumo10 -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Pneumo 10"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Pneumo10-1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- VRH -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="3"
                                Grid.Column="1"
                                Text="VRH"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VRH1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="1ª dose"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="1ª dose"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="1ª dose"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="3"
                            Grid.Column="2"
                            Text="1ª dose"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 3 meses -->
                    <Border
                        IsVisible="{Binding Show3Meses}"
                        Grid.Row="2"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="1"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao3Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="3 meses"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <!-- Meningo C -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Meningo C"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="MeningoC1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="2"
                                Text="1ª dose"
                                Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 4 meses -->
                    <Border
                        IsVisible="{Binding Show4Meses}"
                        Grid.Row="3"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*,*,*,*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="4"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao4Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="4 meses"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <!-- Penta -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Penta"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Penta2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- VIP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="VIP"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VIP2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Pneumo10 -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Pneumo 10"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Pneumo10-2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- VRH -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="3"
                                Grid.Column="1"
                                Text="VRH"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VRH2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="2ª dose"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="2ª dose"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="2ª dose"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="3"
                            Grid.Column="2"
                            Text="2ª dose"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 5 meses -->
                    <Border
                        IsVisible="{Binding Show5Meses}"
                        Grid.Row="4"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="1"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao5Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="5 meses"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Meningo C"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="MeningoC2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="2"
                                Text="2ª dose"
                                Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 6 meses -->
                    <Border
                        IsVisible="{Binding Show6Meses}"
                        Grid.Row="5"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*,*,*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="4"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao6Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="6 meses"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <!-- Penta -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Penta"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Penta3"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- VIP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="VIP"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VIP3"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Covid -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Covid 19"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Covid1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="2"
                                Text="3ª dose"
                                Margin="0,5,0,0"/>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="2"
                                Text="3ª dose"
                                Margin="0"/>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="2"
                                Text="1ª dose"
                                Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 7 meses -->
                    <Border
                        IsVisible="{Binding Show7Meses}"
                        Grid.Row="6"
                        Margin="15,15,15,0"
                        StrokeThickness="0"
                        StrokeShape="RoundRectangle 20">

                        <Grid
                            RowDefinitions="*"
                            ColumnDefinitions="*,*,*"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                                Grid.Column="0"
                                Grid.RowSpan="1"
                                BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao7Meses}"
                                StrokeShape="RoundRectangle 20">

                                <Label
                                    Text="7 meses"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center"
                                    TextColor="White"
                                    FontSize="16"
                                    FontAttributes="Bold"/>

                            </Border>

                            <!-- Covid -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Covid 19"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Covid2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="2"
                                Text="2ª dose"
                                Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 9 meses -->
                    <Border
                    IsVisible="{Binding Show9Meses}"
                    Grid.Row="7"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="1"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao9Meses}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="9 meses"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- Febre amarela -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Febre amarela"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="FA1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 12 meses -->
                    <Border
                    IsVisible="{Binding Show1Ano}"
                    Grid.Row="8"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*,*,*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="4"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao12Meses}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="12 meses"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- Pneumo10 -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Pneumo 10"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Pneumo10-3"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Meningo C -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="Meningo C"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="MeningoC3"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Triplice viral -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Tríplice viral"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="TripliceViral1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="Reforço"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Reforço"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="1ª dose"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 15 meses -->
                    <Border
                    IsVisible="{Binding Show15Meses}"
                    Grid.Row="9"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*,*,*,*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="4"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao15Meses}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="15 meses"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- DTP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="DTP"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="DTP1"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- VIP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="VIP"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="VIP4"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Hepatite A -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Hepatite A"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="HA"/>
                                </Label.GestureRecognizers>

                            </Label>
                            
                            <!-- Tetraviral -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="3"
                                Grid.Column="1"
                                Text="Tetraviral"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="TetraViral"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="1º reforço"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Reforço"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="3"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 4 anos -->
                    <Border
                    IsVisible="{Binding Show4Anos}"
                    Grid.Row="10"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*,*,*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="4"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao4Anos}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="4 anos"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- DTP -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="DTP"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="DTP2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Febre amarela -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="Febre amarela"
                                Margin="0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="FA2"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Varicela -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="2"
                                Grid.Column="1"
                                Text="Varicela"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Varicela"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="2ª reforço"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Reforço"
                            Margin="0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="2"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 5 anos -->
                    <Border
                    IsVisible="{Binding Show5Anos}"
                    Grid.Row="11"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*,*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="2"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao5Anos}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="5 anos"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- Febre amarela -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="Febre amarela"
                                Margin="0,5,0,0">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="FA3"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <!-- Pneumo 23 -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="Pneumo 23"
                                Margin="0,0,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="Pneumo23"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="0"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0,5,0,0"/>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Duas doses"
                            Margin="0,0,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 7 anos -->
                    <Border
                    IsVisible="{Binding Show7Anos}"
                    Grid.Row="12"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="1"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao7Anos}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="7 anos"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- DT -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="DT"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="DT"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Três doses"
                            Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                    <!-- 9 anos -->
                    <Border
                    IsVisible="{Binding Show9Anos}"
                    Grid.Row="13"
                    Margin="15,15,15,0"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 20">

                        <Grid
                        RowDefinitions="*"
                        ColumnDefinitions="*,*,*"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Surface}, Dark={StaticResource DarkSurface}}">

                            <Border
                            Grid.Column="0"
                            Grid.RowSpan="1"
                            BackgroundColor="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.Situacao9Anos}"
                            StrokeShape="RoundRectangle 20">

                                <Label
                                Text="9 anos"
                                HorizontalOptions="Center"
                                VerticalOptions="Center"
                                TextColor="White"
                                FontSize="16"
                                FontAttributes="Bold"/>

                            </Border>

                            <!-- HPV -->
                            <Label
                                Style="{StaticResource VaccineItems}"
                                Grid.Row="0"
                                Grid.Column="1"
                                Text="HPV"
                                Margin="0,5,0,5">

                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.OpenVaccineInfo}"
                                        CommandParameter="HPV"/>
                                </Label.GestureRecognizers>

                            </Label>

                            <Label
                            Style="{StaticResource VaccineItems}"
                            Grid.Row="1"
                            Grid.Column="2"
                            Text="Uma dose"
                            Margin="0,5,0,5"/>

                        </Grid>
                    </Border>

                </Grid>
    </VerticalStackLayout>
</ContentView>